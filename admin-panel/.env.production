# Production builds are served by the same Express app that serves the API
# (see app.js's `/admin` static route), so the API is same-origin — no need
# for the VPS IP/host here, and no CORS_ORIGIN entry is needed for it either.
VITE_API_BASE_URL=/api/v1
